# Some UI libs (cmdk, vaul, react-day-picker, react-resizable-panels...) still
# declare React 18 as a peer but work fine with React 19. Allow install to
# proceed without forcing breaking major upgrades of those components.
legacy-peer-deps=true
